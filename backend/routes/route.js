import { Router } from "express";
import { boardHandler } from "../middlewares/BoardMiddleware";
import 

const router = Router();

// TODO: routes here

// router.get('/', (req, res) => {
//     res.send('Hello World');
// });

router.get('/board', );

export default router;